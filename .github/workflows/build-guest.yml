name: Build guest image

# Builds the Alpine aarch64 guest disk image (spec section 4) and publishes
# it under the rolling "guest-latest" release. The iOS build fetches these
# assets and embeds them in the app bundle.

on:
  push:
    branches: [main]
    paths:
      - "guest/**"
      - ".github/workflows/build-guest.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build alpine-claude.qcow2
    # ubuntu-24.04-arm is GitHub's free arm64 runner for public repos.
    # v0.6.0's earlier attempts on ubuntu-latest x86_64 + qemu-user
    # binfmt hit the 45-min workflow timeout on the Debian package
    # install alone - every dpkg call was emulated. Native arm64
    # brings the build back to ~2-3 min.
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Debian aarch64 rootfs (native arm64)
        run: |
          mkdir -p out
          docker run --rm \
            -v "$PWD/guest:/guest:ro" \
            -v "$PWD/out:/out" \
            debian:12-slim bash /guest/build-rootfs.sh
          # Container writes as root; make outputs readable for the
          # release upload step.
          sudo chmod -R a+r out

      - name: Create qcow2 disk image
        run: |
          sudo apt-get update -q
          sudo apt-get install -y -q qemu-utils zstd e2fsprogs
          sudo mkdir -p rootfs
          sudo tar -xpf out/rootfs.tar -C rootfs
          # 2 GB virtual disk (spec section 3: headroom for claude
          # auto-updates + caches); qcow2 stays thin.
          truncate -s 2G disk.ext4
          sudo mkfs.ext4 -q -L alpine -d rootfs disk.ext4
          qemu-img convert -f raw -O qcow2 disk.ext4 alpine-claude.qcow2
          zstd -19 -T0 alpine-claude.qcow2 -o alpine-claude.qcow2.zst
          ls -lh alpine-claude.qcow2 alpine-claude.qcow2.zst out/

      - name: Read guest metadata
        id: variant
        run: |
          echo "variant=$(cat out/claude-variant.txt 2>/dev/null || echo unknown)" >> "$GITHUB_OUTPUT"
          echo "version=$(cat out/claude-version.txt 2>/dev/null || echo unknown)" >> "$GITHUB_OUTPUT"
          echo "guest_os=$(cat out/guest-os.txt 2>/dev/null || echo unknown)" >> "$GITHUB_OUTPUT"

      - name: System-mode QEMU boot smoke test
        # Boots the packed qcow2 exactly the way the iOS app does
        # (direct kernel, virtio-net-device MMIO, virtio-serial control
        # channel, unix-socket console + control chardevs). The .profile
        # runs `claude --version` at auto-login; the driver watches the
        # console for either the version line (PASS) or one of the
        # known Bun-crash markers (FAIL). This is the definitive test -
        # if it works here, it works on device, because the interpreter
        # backend, kernel, initramfs, rootfs, and userspace are the
        # same. Only speed differs.
        run: |
          sudo apt-get install -y -q qemu-system-arm python3
          python3 guest/smoke-test-qcow2.py \
            alpine-claude.qcow2 out/vmlinuz-virt out/initramfs-virt

      - name: Publish guest-latest release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: guest-latest
          name: "Guest image (rolling)"
          body: |
            Alpine aarch64 guest image for PocketDev, built from guest/.

            guest_os:       ${{ steps.variant.outputs.guest_os }}
            claude_variant: ${{ steps.variant.outputs.variant }}
            claude_version: ${{ steps.variant.outputs.version }}

            Commit: ${{ github.sha }}
            Run: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Assets:
            - alpine-claude.qcow2.zst - root disk (zstd), decompress before use
            - vmlinuz-virt / initramfs-virt - direct kernel boot files
          files: |
            alpine-claude.qcow2.zst
            out/vmlinuz-virt
            out/initramfs-virt
          prerelease: true
          make_latest: false
          draft: false
